<odoo>
    <data>
        <!-- Product Variants Template -->
<!--template inherit_id="website_sale.product" id="css-variant">
<xpath expr="//link[@rel='canonical']" position="after">
        
            <style type="text/css" >

<   div.item-pastille {
      text-align: center;
      display: inline-block;
      width: 80px;
      
      margin: 10px auto;

    }

    label.pastille {
      margin: 20 auto;
      width: 55px;
      height: 55px;
      background: #fdd;
      -moz-border-radius: 20px;
      -webkit-border-radius: 50px;
      border-radius: 80px;
      border: 1px solid #999999;
      text-align: center;

    }
label.pastille input {
  margin: 8px;
  height: 13px;
  opacity: 0;
}
label.pastille.active {
  border: 10px ridge #66ee66;
}
label.pastille.active input {
  margin: 6px;
}


    div.value-pastille {
      margin: 0 auto;
      width: 80px;
      font-size: 0.9em;
      line-height: 1.5em;
      text-align: center;

    }

.pastille {
    display: inline-block;
    border: 1px solid #999999;
    text-align: center;

    input {
        margin: 8px;
        height: 13px;
        opacity: 0;
    }

    .active {
        border: 3px ridge #66ee66;
    }

    .active input {
        margin: 6px;
    }

    .custom_value {
        background-image: linear-gradient(to bottom right, #FF0000, #FFF200, #1E9600);
    }
}




</style>
        
    
    
    </xpath>
</template-->

        <template id="variants" inherit_id="sale.variants">


         <xpath expr="//label[contains(@t-attf-class, 'css_attribute_color')]" position="replace">


                   <!--label t-attf-style="background-color:#{value_id.html_color or value_id.name}"
                          t-attf-class="css_attribute_color #{'active' if not inc else ''}"-->

                     <t t-if="value_id.image">
                    <label  t-attf-style="background:url(data:image/png;base64;base64,{{value_id.image or value_id.name}})"  t-attf-class="1css_attribute_color #{'active' if not inc else ''}">
                    <!--img class="img img-responsive" t-attf-src="data:image/jpg;base64,{{value_id.image}}"  t-attf-class="css_attribute_color #{'active' if not inc else ''} " -->
  <input type="radio" t-att-class="'js_variant_change' if variant_id.attribute_id.create_variant else None"
                          t-att-checked="'checked' if not inc else None"
                          t-att-name="'attribute-%s-%s' % (product.id, variant_id.attribute_id.id)"
                          t-att-value="value_id.id"
                          t-att-title="value_id.name"/>
                          
              
                    <!--/img-->
                   </label>
                   </t>

                    
              <t t-if="not value_id.image">

               <label class="pastille" t-attf-style="background-color:#{value_id.html_color or value_id.name}">
                        <input type="radio" t-att-class="'js_variant_change' if variant_id.attribute_id.create_variant else None"
                          t-att-checked="'checked' if not inc else None"
                          t-att-name="'attribute-%s-%s' % (product.id, variant_id.attribute_id.id)"
                          t-att-value="value_id.id"
                          t-att-title="value_id.name"/>
                         
               </label>
              
              </t>
             <div class="value-pastille" t-field="value_id.name"/>

          </xpath>
        </template>
    </data>
</odoo>

